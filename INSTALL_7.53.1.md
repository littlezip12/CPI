# WPI 7.53.1 Installation

Install the root-level patch over the current WPI 7.53.0 repository, then run `./release-check`.

This release adds a unified tournament-history section to every team profile. Existing ranking, identity, logo, website, JO placement, and tournament source data are preserved.
