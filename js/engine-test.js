(function(){const api=window.CPI_API,$=id=>document.getElementById(id);function num(v){return Number(v).toLocaleString()}const c=api.manifest.counts;const metrics=[["Teams audited",c.teams_audited],["Verified games",c.verified_games],["One-game teams",c.teams_with_one_game],["Unresolved clues",c.unresolved_strings]];$("metricGrid").innerHTML=metrics.map(([l,v])=>`<article class="metric-card"><strong>${num(v)}</strong><span>${l}</span></article>`).join("");function renderDetail(t){$("selectedName").textContent=t.team;$("teamDetail").innerHTML=`<div class="detail-grid"><div class="mini"><h3>Summary</h3><p><strong>${t.record}</strong> · ${t.games} games · GF ${t.goals_for} / GA ${t.goals_against}</p><p>${t.events_seen}</p></div><div class="mini"><h3>Aliases</h3><ul>${t.aliases_list.slice(0,12).map(a=>`<li>${a.alias} (${a.count})</li>`).join("")}</ul></div><div class="mini"><h3>Raw Strings</h3><ul>${t.raw_strings_list.slice(0,12).map(a=>`<li>${a.raw} (${a.count})</li>`).join("")}</ul></div><div class="mini"><h3>Games</h3>${t.games_list.slice(0,20).map(g=>`<div class="game-line"><span>${g.result} ${g.score} vs ${g.opponent}</span><span>${g.event}</span></div>`).join("")}</div></div>`}const focus=api.focusTeams||[];$("focusCards").innerHTML=focus.map((t,i)=>`<article class="focus-card" data-focus="${i}"><h3>${t.team}</h3><strong>${t.record}</strong><span>${t.games} games · ${t.events.length} events</span></article>`).join("");document.querySelectorAll("[data-focus]").forEach(el=>el.addEventListener("click",()=>renderDetail(focus[Number(el.dataset.focus)])));const teams=api.teams||[];$("teamCount").textContent=`${teams.length} teams`;$("teamTable").innerHTML=teams.slice(0,220).map((t,i)=>`<tr class="team-row" data-team="${i}"><td>${t.team}</td><td>${t.club}</td><td>${t.record}</td><td>${t.games}</td><td>${t.goals_for}</td><td>${t.goals_against}</td><td>${t.alias_count}</td><td>${t.raw_string_count}</td></tr>`).join("");document.querySelectorAll("[data-team]").forEach(el=>el.addEventListener("click",()=>renderDetail(teams[Number(el.dataset.team)])));if(focus[0]) renderDetail(focus[0]);})();