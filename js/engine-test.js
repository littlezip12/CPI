(function(){const api=window.CPI_API,$=id=>document.getElementById(id);function num(v){return Number(v).toLocaleString()}const c=api.manifest.counts;const metrics=[["Unique raw",c.unique_raw_strings],["Changed",c.changed_by_pipeline],["Ignored",c.ignored_placeholders],["Registry matches",c.registry_matches_prepped],["Suspicious",c.still_suspicious]];$("metricGrid").innerHTML=metrics.map(([l,v])=>`<article class="metric-card"><strong>${num(v)}</strong><span>${l}</span></article>`).join("");const rows=api.parserDebugRows||[];$("rowCount").textContent=`${rows.length} rows`;$("debugTable").innerHTML=rows.slice(0,200).map(r=>`<tr><td>${r.raw}</td><td>${r.stage_2_placement}</td><td>${r.stage_3_bracket}</td><td>${r.stage_4_seed}</td><td>${r.final}</td><td>${r.registry_match||"—"}</td><td>${r.status}</td></tr>`).join("");})();