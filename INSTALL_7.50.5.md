# Install CPI 7.50.5 — JO Full-Round Journey Continuity

Extract this patch into the CPI repository root, remove `.DS_Store`, and run `./release-check`.

The patch changes only the Boys/Girls JO viewer code, related validation, and release metadata. It does not include generated tournament data.
