# WPI 7.53.4 Installation

Install the patch over the WPI 7.53.3 repository, run `./release-check`, and commit only when the final line is `CPI release check passed.`

Suggested commit message:

`WPI 7.53.4 — complete branding and restore section home pages`
