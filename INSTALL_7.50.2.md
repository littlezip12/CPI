# Install CPI 7.50.2 — Canonical Logo Delivery Hotfix

Extract this patch into the CPI repository root, then run:

```bash
cd "/Users/tylerdeshazer/Documents/GitHub/CPI"
find . -name ".DS_Store" -delete
./release-check
```

Do not commit or push unless the final line is `CPI release check passed.`
