# Install WPI 7.53.3

Extract the release patch over the repository root, remove macOS metadata, and run `./release-check`.

Commit only after the final line is exactly:

```text
CPI release check passed.
```

Suggested commit message:

```text
WPI 7.53.3 — clarify navigation and homepage pathways
```
