# Install WPI 7.56.0

Apply this revised cumulative ZIP to the committed WPI repository, run `./release-check`, and commit only when the final line is exactly:

```text
CPI release check passed.
```

This revision supersedes the earlier unpushed 7.56.0 ZIP. It changes only the private live-scoring sandbox and release validation. It does not alter rankings, tournament results, identities, logos, or public player information.
