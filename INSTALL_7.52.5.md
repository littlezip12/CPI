# CPI 7.52.5 — Club Location & Region Completion

Completes location and regional metadata for every club previously marked `Region TBD`.

Run `./release-check` after applying the patch. Do not commit or push unless the final line is:

`CPI release check passed.`
