# WPI 7.53.2 Installation

Install the root-level patch over the current WPI 7.53.1 repository, then run `./release-check`.

This release establishes centralized sponsor inventory and rendering infrastructure. It ships with zero active campaigns, so no sponsor card appears until a real approved partner is added. Rankings, club identities, websites, logos, tournament history, and JO data are preserved.
