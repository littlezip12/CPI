# Install WPI 7.54.18

This cumulative patch applies the approved club-location audit and launches the national club-region foundation.

It introduces Hawaii and six directional continental U.S. regions, classifies Barcelona Lions as International, adds a real state-boundary map, and synchronizes the approved location fields across every dependent WPI system.

Run the complete release check after installation. Commit only when the final line is exactly:

`CPI release check passed.`

Back Bay is synchronized as Irvine, California in the Orange County region.
