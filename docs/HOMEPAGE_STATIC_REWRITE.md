# CPI Static Homepage Rewrite

This removes dynamic homepage assembly and restores a deterministic page order:
Header -> Hero -> Top Stories -> At-a-Glance -> Subscribe -> Footer.

Only `homepage-static.js` handles small enhancements: hero carousel and filling At-a-Glance rows from `data.js`.
