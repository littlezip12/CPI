# Install WPI 7.55.9

Apply the cumulative ZIP to the committed WPI repository, run `./release-check`, and commit only after the exact final line `CPI release check passed.`

This release refines only the hidden live-scoring sandbox interface. Rankings, official tournament data, identities, and immutable season snapshots remain unchanged.
