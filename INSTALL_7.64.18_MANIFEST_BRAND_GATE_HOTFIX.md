# WPI 7.64.18 — Release Manifest Brand Gate Hotfix

This hotfix removes the legacy repository filename reference `CPI-main` from the public root release manifest while preserving the source baseline SHA-256 and all release metadata.

It also refreshes manifest file hashes/byte counts against the current 7.64.18 public-game hotfix baseline.

No application runtime, database, Supabase, GroupMe, scoring, rankings, analytics, or PWA behavior changes are included.
